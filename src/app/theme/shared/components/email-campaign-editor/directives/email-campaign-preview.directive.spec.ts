import { EmailCampaignPreviewDirective } from './email-campaign-preview.directive';

describe('EmailCampaignPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailCampaignPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
