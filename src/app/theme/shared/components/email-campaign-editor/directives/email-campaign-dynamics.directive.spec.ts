import { EmailCampaignDynamicsDirective } from './email-campaign-dynamics.directive';

describe('EmailCampaignDynamicsDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailCampaignDynamicsDirective();
    expect(directive).toBeTruthy();
  });
});
