import { EmailCampaignTemplatesDirective } from './email-campaign-templates.directive';

describe('EmailCampaignTemplatesDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailCampaignTemplatesDirective();
    expect(directive).toBeTruthy();
  });
});
