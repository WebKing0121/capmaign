import { DatatableClassnamePipe } from './datatable-classname.pipe';

describe('DatatableClassnamePipe', () => {
  it('create an instance', () => {
    const pipe = new DatatableClassnamePipe();
    expect(pipe).toBeTruthy();
  });
});
