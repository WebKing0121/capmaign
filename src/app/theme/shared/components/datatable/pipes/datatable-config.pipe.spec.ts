import { DatatableConfigPipe } from './datatable-config.pipe';

describe('DatatableConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new DatatableConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
