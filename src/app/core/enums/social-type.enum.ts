export enum SocialType {
  Twitter = 0,
  Facebook = 1,
  GooglePlus = 2,
  LinkedIn = 3,
}

export enum SocialLabelType {
  Facebook = 'FaceBook',
  LinkedIn = 'LinkedIn',
  Twitter = 'Twitter'
}

export enum SocialAccountType {
  Profile = 0,
  Page = 1,
  Group = 2
}

