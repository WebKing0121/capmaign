export class Event {
  id: number | string;
  name: string;
  subject: string;
  // tslint:disable-next-line
  start_date: string;
  // tslint:disable-next-line
  end_date: string;
  // tslint:disable-next-line
  display_name: string;
  folder: string;
  location: string;
  template: string;
}
